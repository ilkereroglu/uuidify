{
  "name": "uuidify-status",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx serve out"
  },
  "engines": {
    "node": ">=18"
  }
}
